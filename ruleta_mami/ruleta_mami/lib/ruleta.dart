import 'package:flutter/material.dart';

class Ruleta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Update this widget with actual UI components
    return Container();
  }
}
